namespace PlaywrightSharp.Transport.Channel
{
    internal class FrameChannel : Channel
    {
        public FrameChannel(string guid, ConnectionScope scope) : base(guid, scope)
        {
        }
    }
}
